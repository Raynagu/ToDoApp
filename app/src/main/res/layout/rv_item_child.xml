<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <CheckBox
        android:id="@+id/cb_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:layout_toStartOf="@+id/iv_editItem"
        android:layout_toLeftOf="@+id/iv_editItem"/>
    <ImageView
        android:id="@+id/iv_editItem"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="8dp"
        android:src="@drawable/ic_edit_black_48dp"
        android:layout_toStartOf="@+id/iv_deleteItem"
        android:layout_toLeftOf="@+id/iv_deleteItem"/>
    <ImageView
        android:id="@+id/iv_deleteItem"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="8dp"
        android:src="@drawable/ic_delete_forever_black_48dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"/>


</RelativeLayout>